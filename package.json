{
  "name": "QuanLyMuonSach",
  "version": "1.0.0",
  "description": "Library Management System using MEVN stack",
  "scripts": {
    "start": "node Server/server.js",
    "server": "cd Server && npm run dev",
    "client": "cd Client && npm run serve",
    "admin": "cd Admin && npm run serve",
    "install-all": "cd Server && npm install && cd ../Client && npm install && cd ../Admin && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run admin\""
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.10.9",
    "jsonwebtoken": "^8.5.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "concurrently": "^7.6.0"
  }
}